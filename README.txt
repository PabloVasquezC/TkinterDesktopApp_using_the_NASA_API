Tkinter:    Tkinter es una biblioteca de Python que permite 
            crear interfaces gráficas de usuario de forma sencilla. 
            Incluye una serie de controles básicos, como botones
            etiquetas y cuadros de texto.

Funciones básicas 
de tkinter: .window() // crea una ventana 
            .label() // crea una etiqueta
            .mainloop() // mantiene la ventana abierta 
            .title() // titulo de la ventana 
            .geometry() // tamaño de la ventana 
            .config() // configuracion de la ventana 
            .pack() // empaqueta los elementos en la ventana 
            .place() // coloca los elementos en la ventana 
            .grid() // coloca los elementos en la ventana 
            .bind() // enlaza un evento a un elemento 
            .get() // obtiene el valor de un elemento 
            .set() // establece el valor de un elemento 
            .destroy() // destruye un elemento 
            .update() // actualiza un elemento 
            .insert() // inserta un elemento
            .delete() // elimina un elemento 
            .update_idletasks() // actualiza un elemento
